import { CardHeader, CardTitle } from "@/components/ui/card";
import { SubmitForm } from "./submit-form";

export function Register() {
  return (
    <>
      <CardHeader>
        <CardTitle>Sign up to start listening</CardTitle>
      </CardHeader>
      <SubmitForm />
    </>
  );
}
